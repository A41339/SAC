
@model Entities.Entities.Evaluacion.EvalCategoria

@{
    ViewBag.Title = "EstÃ¡ seguro de eliminar la categorÃ­a?";
    if (Ajax.IsThisAjaxRequest())
    {
        Layout = null;
    }
}

@Html.StarkAjaxFormSubmiter("frmCompany", "tbCompany", true, "AcciÃ³n ejecutada con Ã©xito")

@{
    if (!Ajax.IsThisAjaxRequest())
    {
        <section class="content-header">
    <h1>
        <i class="fa fa-trash"></i> Eliminar Categor&iacute;a
        <small>Confirmaci&oacute;n de eliminaci&oacute;n de la categor&iacute;a de evaluaci&oacute;n.</small>
    </h1>
    <ol class="breadcrumb">
        <li><a href="@Url.Action("Index", "Home")"><i class="fa fa-dashboard"></i> Inicio</a></li>
        <li><a href="@Url.Action("Index", "Modulo", new { modulo = "Evaluacion/Index" })" class="breadcrumb-parent-pill"><i class="fa fa-pencil-square-o" style="margin-right: 4px;"></i> Evaluaci&oacute;n</a></li>
        <li class="active">Eliminar Categor&iacute;a</li>
    </ol>
</section>
    }
}

<section class="content">
    <div class="row">
        <div class="col-xs-12">
            <div class="box box-solid box-info">
                <div class="box-header">
                    <h3 class="box-title">Detalles</h3>
                </div><!-- /.box-header -->
                <div class="box-body table-responsive">
                    <div class="widget-sec">
                        <div class="streaming-table">
                            <table class="table table-striped table-bordered">
                                <tr>
                                    <td>Id</td>
                                    <td>@Html.DisplayFor(model => model.Id)</td>
                                </tr>
                                <tr>
                                    <td>TÃ­tulo</td>
                                    <td>@Html.DisplayFor(model => model.Titulo)</td>
                                </tr>
                            </table>
                            <div class="box-footer">

                                @using (Html.BeginForm("Delete", "Evaluacion", FormMethod.Post, new { id = "frmCompany" }))
                                {
                                    @Html.AntiForgeryToken()
                                    @Html.StarkInputButtion("Borrar", "submit", "btn btn-danger", "")
                                    @Html.ActionLink("Regresar", "Index", null, new { @class = "btn btn-primary" })
                                }
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>